 ### 1. "Solo Mode" Toggle (Auto-Approve)
  We can add a simple toggle in the Project Settings called "Solo Mode (Bypass Review Queue)".

  • When this is turned on, clicking "Save" in the annotation workspace instantly sets the image status to  approved  instead of  pending .
  • The image goes straight to the finished pile, and they never even have to look at the Review page.

  The FCOT structure must be rigidly enforced so corrupted data never pollutes the DB.

  • Action: Update the  <AnnotationCanvas />  to embed the full FCOT block for every drawn box (pulled from the active taxonomy version).
  • Action: Implement a strict Next.js Server Action (using Zod) to validate that every save payload has the exact required FCOT fields and
  valid  [0-1000]  coordinates before touching Supabase.
  • Why: Answers "where is chain of thought in json outputs?" and prevents broken exports.
  ### Milestone 12: Undo/Redo & Autosave Drafts (Canvas Architecture)

  Replicating the speed and safety of the original tool.

  • Action: Implement a reducer in the canvas with  past ,  present , and  future  stacks to support full  Ctrl+Z  (Undo) and  Ctrl+Shift+Z 
  (Redo) for bounding boxes.
  • Action: Implement debounced autosaving to a  drafts  table (bypassing Vercel serverless limits by writing directly via Supabase client)
  so progress is never lost mid-image.
  • Action: Add  localStorage  synchronous fallback for drafts as a belt-and-suspenders backup.

  ### Milestone 13: Scene Context Persistence & Derived Diagnostics

  Matching the annotator speed of the Colab notebook.

  • Action: Ensure Scene Metadata fields (like lighting/turbidity) automatically persist their values across image transitions unless
  manually changed by the annotator.
  • Action: Automatically calculate  derived_diagnostics  (e.g., tallying the most common class as the  primary_pollutant ) upon saving the
  image.

  ### Milestone 14: Client-Side Image Preprocessing & Coordinate Math

  Porting the PIL image resizing logic to the browser.

  • Action: Implement an offscreen canvas to scale original Drive images using the  max_dim  and  jpeg_quality  settings before they are
  rendered on the screen.
  • Action: Normalize all drawn bounding boxes to  ymin_xmin_ymax_xmax_0to1000  format (relative to the scaled image, not the raw DOM
  pixels).

  ### Milestone 15: Review & QA Workflow

  Adding the multi-user QA flow described in the spec.

  • Action: Build the  /projects/[id]/review  page for project owners to see a queue of all  pending  images.
  • Action: Allow reviewers to Approve, Flag (with notes), or Edit & Approve directly over the annotator's work.
  • Action: Log all mutations to the  annotation_history  audit table.

  ### Milestone 16: Multi-Format JSON Zipping (COCO, YOLO, Anytate)

  Handling the CPU-heavy export logic safely on Vercel's Hobby Tier.

  • Action: Build  /api/export  to pull the project's annotations and execute the math conversions ( lib/bboxConvert.ts ).
  • Action: Support COCO (absolute pixels  [x, y, w, h] ), YOLO (normalized centers), and Anytate (True FCOT schema).
  • Action: Use  JSZip  strictly on the client-side browser to bundle the files so Vercel doesn't hit a CPU timeout limit.

  ### Milestone 17: Project Management Settings

  Giving the user control over the project lifecycle.

  • Action: Build a  /projects/[id]/settings  page to allow editing of the  preprocessing  JSONB ( max_dim ,  jpeg_quality ).
  • Action: Implement a cascading Project Deletion feature (safely removing drafts, images, and taxonomy versions).

  ### Milestone 18: Developer/Terminal Aesthetics & Theme Toggle

  The final visual polish layer.

  • Action: Rip out the remaining soft glassmorphism and replace it with the requested rigid, terminal/developer-coded UI (monospaced fonts,
  syntax-highlighted accents like the notebook).
  • Action: Implement  next-themes  for a fully functional, persistent Dark/Light mode toggle.
  ──────
  Does this elaborate plan accurately reflect the full scope of what you want built? If so, we can immediately begin executing Milestone 11.