  Here is the current roadmap of the remaining uncompleted milestones, ensuring we capture everything requested in your  anytate-
  implementation-plan.md  and  human_annotation_final_fix4.ipynb  files:
  ### Milestone 11: True FCOT Data Contract & JSON Export

  • Goal: Ensure every drawn instance and scene context completely abides by the FCOT (Free-text Chain-of-Thought) schema defined in the
  Taxonomy Builder.
  • Tasks: Update the  AnnotationCanvas  to embed the full taxonomy-derived schema into the JSON.
  • Why it matters: This answers your requirement of "where is chain of thought in json outputs?"
  ### Milestone 12: Developer/Terminal Aesthetics & Theme Toggle

  • Goal: Overhaul the current glassmorphism design to a high-end, developer-centric terminal aesthetic.
  • Tasks: Implement a rigid, syntax-highlighted vibe, a mono-spaced focus, and add a fully working Dark/White mode toggle.
  • Why it matters: This answers your requirement of "where is developer coding vibe UI, where is darkmode/whitemode toggle?"

  ### Milestone 13: Project Management Settings

  • Goal: Add robust project lifecycle controls.
  • Tasks: Add the ability to fully delete projects (with cascade confirmation). Add a robust settings page to edit preprocessing variables
  and drive paths post-creation.
  • Why it matters: This answers your requirement of "where is project management (no option to delete project)?"

  ### Milestone 14: Coordinate Normalization

  • Goal: Implement the exact coordinate math used in your Python backend.
  • Tasks: Ensure the canvas normalizes all bounding boxes to  ymin_xmin_ymax_xmax_0to1000  rather than raw canvas pixels.

  ### Milestone 15: Scene Context Persistence & Diagnostics

  • Goal: Streamline annotator speed and match the notebook wrappers.
  • Tasks: Ensure "Global Scene Context" fields can persist across image transitions if toggled. Automatically compute and inject 
  derived_diagnostics  (like object counts, occlusion warnings, etc).

  ### Milestone 16: Exact Export Schema Match

  • Goal: Make the "Anytate JSON" export identical to the Python notebook format.
  • Tasks: Finalize the  /api/export  logic to package the  global_scene_context ,  environmental_diagnostics , and  grounded_instances 
  into the exact JSON tree shape expected by your training scripts.

  ### Milestone 17: Image Preprocessing Port
  • Goal: Bring backend image prep to the frontend.
  • Tasks: Port the  max_dim  scaling and  jpeg_quality  logic into the browser or edge API so images are properly resized according to
  project settings before being annotated.
  ──────
  We just finished the Custom CoT/Taxonomy tools (and provenance). Would you like to dive straight into Milestone 11 (True FCOT Data
  Contract) next?

────────────────────────────────────────────────────────────
> look at the plan again make sure nothing is left out, make a more elaborate milestone plan

▸ Thought for 52s, 350 tokens
  Prioritizing Tool Usage

● Read(C:/Users/Minhaz/Desktop/Code/anytate/anytate-implementation-plan.md) (ctrl+o to expand)

▸ Thought for 5s, 857 tokens
  Prioritizing Tool Usage
● Read(C:/Users/Minhaz/Desktop/Code/anytate/human_annotation_final_fix4.ipynb) (ctrl+o to expand)

▸ Thought for 1s
  Prioritizing Tool Usage
  I have reviewed the entire  anytate-implementation-plan.md  architecture document and the  human_annotation_final_fix4.ipynb  notebook to
  ensure every single detail is captured. You are completely right—the previous list was too brief and missed a few crucial architectural
  steps.

  Here is the exhaustive, elaborate milestone plan for everything that remains to be built to reach 100% feature parity (and superiority)
  over the original tool:
  ### Milestone 11: True FCOT Data Contract & Server-Side Validation

  The FCOT structure must be rigidly enforced so corrupted data never pollutes the DB.

  • Action: Update the  <AnnotationCanvas />  to embed the full FCOT block for every drawn box (pulled from the active taxonomy version).
  • Action: Implement a strict Next.js Server Action (using Zod) to validate that every save payload has the exact required FCOT fields and
  valid  [0-1000]  coordinates before touching Supabase.
  • Why: Answers "where is chain of thought in json outputs?" and prevents broken exports.
  ### Milestone 12: Undo/Redo & Autosave Drafts (Canvas Architecture)

  Replicating the speed and safety of the original tool.

  • Action: Implement a reducer in the canvas with  past ,  present , and  future  stacks to support full  Ctrl+Z  (Undo) and  Ctrl+Shift+Z 
  (Redo) for bounding boxes.
  • Action: Implement debounced autosaving to a  drafts  table (bypassing Vercel serverless limits by writing directly via Supabase client)
  so progress is never lost mid-image.
  • Action: Add  localStorage  synchronous fallback for drafts as a belt-and-suspenders backup.

  ### Milestone 13: Scene Context Persistence & Derived Diagnostics

  Matching the annotator speed of the Colab notebook.

  • Action: Ensure Scene Metadata fields (like lighting/turbidity) automatically persist their values across image transitions unless
  manually changed by the annotator.
  • Action: Automatically calculate  derived_diagnostics  (e.g., tallying the most common class as the  primary_pollutant ) upon saving the
  image.

  ### Milestone 14: Client-Side Image Preprocessing & Coordinate Math

  Porting the PIL image resizing logic to the browser.

  • Action: Implement an offscreen canvas to scale original Drive images using the  max_dim  and  jpeg_quality  settings before they are
  rendered on the screen.
  • Action: Normalize all drawn bounding boxes to  ymin_xmin_ymax_xmax_0to1000  format (relative to the scaled image, not the raw DOM
  pixels).

  ### Milestone 15: Review & QA Workflow

  Adding the multi-user QA flow described in the spec.

  • Action: Build the  /projects/[id]/review  page for project owners to see a queue of all  pending  images.
  • Action: Allow reviewers to Approve, Flag (with notes), or Edit & Approve directly over the annotator's work.
  • Action: Log all mutations to the  annotation_history  audit table.

  ### Milestone 16: Multi-Format JSON Zipping (COCO, YOLO, Anytate)

  Handling the CPU-heavy export logic safely on Vercel's Hobby Tier.

  • Action: Build  /api/export  to pull the project's annotations and execute the math conversions ( lib/bboxConvert.ts ).
  • Action: Support COCO (absolute pixels  [x, y, w, h] ), YOLO (normalized centers), and Anytate (True FCOT schema).
  • Action: Use  JSZip  strictly on the client-side browser to bundle the files so Vercel doesn't hit a CPU timeout limit.

  ### Milestone 17: Project Management Settings

  Giving the user control over the project lifecycle.

  • Action: Build a  /projects/[id]/settings  page to allow editing of the  preprocessing  JSONB ( max_dim ,  jpeg_quality ).
  • Action: Implement a cascading Project Deletion feature (safely removing drafts, images, and taxonomy versions).

  ### Milestone 18: Developer/Terminal Aesthetics & Theme Toggle

  The final visual polish layer.

  • Action: Rip out the remaining soft glassmorphism and replace it with the requested rigid, terminal/developer-coded UI (monospaced fonts,
  syntax-highlighted accents like the notebook).
  • Action: Implement  next-themes  for a fully functional, persistent Dark/Light mode toggle.
  ──────
  Does this elaborate plan accurately reflect the full scope of what you want built? If so, we can immediately begin executing Milestone 11.